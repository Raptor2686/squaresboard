# Render Build Command (set in Render dashboard → your service → Build Command):
# pip install --force-reinstall --no-cache-dir python-version==3.12.0 && pip install --no-cache-dir -r requirements.txt
#
# IMPORTANT: Set Runtime to Python 3.12 in Render dashboard → Environment → Runtime
# or add this to a post-install hook:
# pip install 'pydantic>=2.0' --force-reinstall

fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
aiosqlite==0.20.0
passlib[bcrypt]==1.7.4
python-dotenv==1.0.1
httpx==0.27.2
apscheduler==3.10.4
stripe==10.4.0
pydantic==2.9.2
pydantic-settings==2.5.2